variables:
  ANDROID_HOME: "/opt/android-sdk"
  LOCAL_PATH: "/home/gitlab-runner/projects/recomiend"
  OUTPUTS_PATH: android/app/build/outputs/apk
  PRODUCTION_BUILD: $OUTPUTS_PATH/app-release.apk

before_script:
  - npm cache clean --force
  - npm install

stages:
  - build
  - deploy

build:
  stage: build
  tags:
    - shell
  script:
    - cd android && ./gradlew app:clean app:build -x test

android-release:
  stage: deploy
  tags:
    - shell
  artifacts:
    paths:
      - android/app/build/outputs/apk
  script:
    - cd android && ./gradlew app:clean
    - react-native bundle 
                      --platform android 
                      --dev false 
                      --entry-file index.js 
                      --bundle-output android/app/src/main/assets/index.android.bundle 
                      --assets-dest android/app/src/main/res &&  app:assembleRelease &&
    - rm -rf app/src/main/res/drawable-* &&
    - cd android && ./gradlew app:assembleRelease

  when: manual
